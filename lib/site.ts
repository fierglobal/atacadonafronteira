// Fonte única do domínio público. Trocar aqui quando o domínio definitivo
// do Atacado na Fronteira for confirmado (hoje é placeholder).
export const SITE_URL =
  process.env.NEXT_PUBLIC_SITE_URL?.replace(/\/$/, '') || 'https://atacadonafronteira.app'

export const SITE_NAME = 'Atacado na Fronteira'
